# Este arquivo vazio garante que a pasta `.idea` seja versionada no controle
# de código fonte.  As configurações específicas do IntelliJ serão geradas
# automaticamente quando o projeto for importado e não fazem parte do
# código fonte da aplicação.